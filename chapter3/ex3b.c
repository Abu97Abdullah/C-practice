// Used this to verify what I wrote in ex3.txt for (b)
#include <stdio.h>

int main(void)
{
    int a, b, c, d, e, f;

    scanf("%d -%d -%d", &a, &b, &c);

    printf("\n%d, %d, %d\n", a, b, c);

    scanf("%d-%d-%d", &d, &e, &f);

    printf("\n%d, %d, %d\n", d, e, f);

    return 0;
}